# 盤前預估分數（用美股隔夜 + 台股夜盤）

目的：在台股**開盤前**（約早上 08:00）就先給一個「**盤前預估分數**」，
讓使用者不必等收盤才知道今天大概偏多還偏空。收盤後仍照舊算出**正式分數**覆蓋。

> 這份是給**評分引擎（Stock Tracker Bot）**用的規格。引擎目前每天收盤後算一次正式分數；
> 本規格要新增「每天早上先算一次盤前預估」這條路徑。引擎其實**已經在抓美股/ADR 資料**
> （新聞頁就有「美股盤後、台積電 ADR 跌 2.24%」），所以資料管線多半現成、只是沒拿來算分。

⚠️ 盤前分數是**預測 / 風向**，不是收盤後的真實分數，**更不是買賣訊號**。
UI 一定要標清楚「盤前預估・非正式」，否則就會被當成即時明牌（責任與信任問題）。

---

## 1. 一天跑兩次

| 版本 | 台灣時間 | 用什麼算 | 標籤 |
|------|---------|---------|------|
| **盤前預估版** | 約 **08:00**（開盤前） | 昨日收盤分數 + 美股隔夜 + 台股夜盤偏移 | `盤前預估・非正式` |
| **收盤正式版**（現有，不變） | 收盤後 | 完整四大支柱真實數據 | （正式，無標籤） |

> 排程建議：盤前版用 `cron` 設在台灣 08:00（= UTC 00:00）跑；正式版維持現有排程。

---

## 2. 盤前要抓的領先指標（依影響力排序）

| 因子 | 代號 / 來源 | 取什麼 | 建議權重 |
|------|-----------|-------|---------|
| **台積電 ADR** | `TSM`（Yahoo Finance，美股） | 昨夜收盤漲跌 % | **0.45** |
| **台指期夜盤** | 台灣期交所盤後（15:00–次日05:00） | 夜盤漲跌 % | 0.25 |
| **費城半導體** | `^SOX` | 昨夜漲跌 % | 0.15 |
| **S&P500 / 那斯達克** | `^GSPC` / `^IXIC` | 昨夜漲跌 % | 0.15 |
| （參考）美元台幣 | `TWD=X` | 走勢方向 | 微調，可不計分 |

> **台指期夜盤若抓不到**（Yahoo 沒有期交所夜盤資料）：把它的 0.25 權重**按比例分回** ADR 與 SOX
> （ADR→0.60、SOX→0.25、指數→0.15）。台積電 ADR + 費半其實已能代表 8 成隔夜方向。

---

## 3. 算法（最小改動版：偏移量疊加）

不重算四大支柱，只用隔夜訊號算一個「盤前偏移量」加到昨日收盤分數上：

```
# 1) 加權隔夜訊號（單位：%）
OVN = 0.45*ADR%  +  0.25*TXF_night%  +  0.15*SOX%  +  0.15*SPX%
#    （TXF 抓不到時改用 0.60*ADR% + 0.25*SOX% + 0.15*SPX%）

# 2) 映射成分數偏移（K 為敏感度，先設 3；±12 為上下限避免暴衝）
delta = clip(OVN * 3, -12, +12)

# 3) 盤前預估分數
premarket_score = clip(round(yesterday_composite + delta), 0, 100)
```

- `K=3`：隔夜每 +1% ≈ +3 分；+2% ≈ +6 分，封頂 ±12 分。`K` 可依日後回測微調。
- `yesterday_composite` = 昨日收盤的正式 composite（例：44.7）。

> **進階版（更正確、之後可選）**：不要用偏移量，而是直接用隔夜數據**覆寫「趨勢・技術面」那根支柱**
> （它本來就含 S&P500），其餘三根沿用昨收值，再依原本權重重算 composite。
> 先上「偏移量疊加」最快、最不會動到既有計分邏輯。

---

## 4. 輸出欄位

盤前版執行時，在資料裡多寫這幾個欄位（給 `index.html` 用）：

| 欄位 | 範例 | 說明 |
|------|------|------|
| `premarket_score` | `38` | 盤前預估分數（整數） |
| `premarket_delta` | `-7` | 相對昨收的偏移量 |
| `premarket_time` | `2026-06-05 08:00` | 盤前版產生時間（台灣） |
| `premarket_basis` | `ADR -2.24% / 道瓊 -620 / SOX -2.0%` | 算分依據（顯示給使用者看的一句話） |
| `is_premarket` | `true` | 目前顯示的是否為盤前版 |

收盤正式版執行時把 `is_premarket` 設回 `false`，正式分數覆蓋顯示。

---

## 5. UI 規則（`index.html`）

- 盤前時段（`is_premarket=true`）：主徽章旁加一個小標籤
  **「盤前預估・非正式」**，顏色用灰／藍中性色（**不要**用正式分數的紅綠，避免被當訊號）。
- 標籤下方一行小字：`依昨晚美股 + 夜盤估算，收盤後更新正式分數。{premarket_basis}`
- 收盤後（`is_premarket=false`）：恢復成現在的正式徽章，移除盤前標籤。
- 區間標籤 / 倍數沿用 `SCORING_LABELS.md` 同一張對照表（盤前分數也查同表，口徑一致）。

---

## 6. 套用後的效果（以今天 6/05 為例，數字為示意）

昨日收盤正式分數 **44.7（≈45，中性區）**。昨晚美股收黑：
台積電 ADR −2.24%、道瓊 −620、標普止步九連漲、費半走弱。

代入算法（示意）：`OVN ≈ -2.2%` → `delta ≈ -7` → **盤前預估 ≈ 38**。

→ 開盤前就會顯示：
**「盤前預估 38・非正式」**＋「中性偏空，接近便宜區；依昨晚美股 + 夜盤估算，收盤後更新正式分數。」

正好對應「跌爛欸」的盤面：**不必等收盤，早上就知道今天偏冷、分數正往便宜區掉。**

---

## 7. 備註

- `K`、各因子權重、±12 上限都可調；建議日後用「隔夜訊號 → 當日收盤分數變化」做一次回測校準。
- 盤前版**只跑大盤分數**即可（個股/籌碼等盤中無新資料，沿用昨收）。
- ⚠️ 所有對外文案維持「非投資建議」基調；盤前分數務必標「預估・非正式」，不喊明牌、不報目標價。
